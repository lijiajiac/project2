<?xml version="1.0" encoding="UTF-8"?>
<!DOCTYPE mapper PUBLIC "-//mybatis.org//DTD Mapper 3.0//EN" "http://mybatis.org/dtd/mybatis-3-mapper.dtd">
<mapper namespace="com.yida.project.mapper.UserMapper">

    <resultMap type="com.yida.project.entity.User" id="UserMap">
        <result property="id" column="id" jdbcType="INTEGER"/>
        <result property="name" column="name" jdbcType="VARCHAR"/>
        <result property="password" column="password" jdbcType="VARCHAR"/>
        <result property="state" column="state" jdbcType="INTEGER"/>
        <result property="salt" column="salt" jdbcType="VARCHAR"/>
        <result property="createdate" column="createDate" jdbcType="TIMESTAMP"/>
        <collection property="roles" javaType="java.util.Set" ofType="com.yida.project.entity.Role">
            <id property="id" column="rid"></id>
            <result property="name" column="rname"></result>
        </collection>
        <!--    <collection property="userRoles" javaType="java.util.Set" ofType="com.yida.project.entity.UserRole">
            <id property="id" column="rid"></id>
            <result property="userId" column="user_id"></result>
            <result property="roleId" column="role_id"></result>
             </collection>-->
    </resultMap>


    <select id="selectName" resultMap="UserMap">
        /*关联查询*/
        SELECT u.*,r.id rid,r.name rname FROM user u
        INNER JOIN user_role ur ON u.id = ur.user_id
        INNER JOIN role r ON r.id = ur.role_id
        WHERE u.name = #{name};
    </select>

    <insert id="adduser">
        insert into user_role (user_id,role_id)
        values (
        #{userid},#{roleid}
        )
    </insert>

    <select id="user_select" resultMap="UserMap">
        select id from user_role where user_id = #{uid}
    </select>

</mapper>


